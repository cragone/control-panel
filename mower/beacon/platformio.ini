; PlatformIO Project Configuration File — WiFi localization beacon
;
; Flash this to each fixed beacon board (mower/src/main.cpp is the rover).
; See ../docs/localization.md for placement and calibration.

[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
upload_speed = 115200
